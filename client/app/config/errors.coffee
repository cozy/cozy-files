# Associate errors type with its translated key
module.exports = {
    folderError: 'modal error firefox dragdrop folder'
    existingFolderError: 'modal error existing folder'
    maxSizeExceeded: 'modal error updoad size exceed'
}
