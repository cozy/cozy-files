// Generated by CoffeeScript 1.7.1
var americano, errorHandler, options;

americano = require('americano');

errorHandler = require('./server/middlewares/errors');

options = {
  name: 'cozy-files',
  root: __dirname,
  port: process.env.PORT || 9121,
  host: process.env.HOST || '127.0.0.1'
};

americano.start(options, function(app, server) {
  var RealtimeAdapter, realtime;
  app.server = server;
  app.use(errorHandler);
  RealtimeAdapter = require('cozy-realtime-adapter');
  return realtime = RealtimeAdapter({
    server: server
  }, ['file.*', 'folder.*', 'contact.*']);
});
