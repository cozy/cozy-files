td
  if model.type && model.type == "folder"
    a.img-folder
      img(src="images/folder.png")
  else
    span.glyphicon.glyphicon-file.no-hover.icon
  input.caption.file-edit-name(value=model.name)
  a.btn.btn-sm.btn-cozy.file-edit-save
    | #{t("file edit save")}
  a.btn.btn-sm.btn-link.file-edit-cancel
    | #{t("file edit cancel")}
td.file-size
    - options = {base: 2}
    span.pull-left #{filesize(model.size || 0, options)}
td.file-type.type-column-cell
    span.pull-left #{t(model.class)}
td.file-date.date-column-cell
  if model.lastModification
      span.pull-left #{moment(model.lastModification).calendar()}
